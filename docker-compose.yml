services:
  backend:
    build: backend/
    container_name: riichi-backend
    ports:
      - "8080:8080"

  frontend:
    build: frontend/
    container_name: riichi-frontend
    ports:
      - "3000:3000"